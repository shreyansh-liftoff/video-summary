'use client'

import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#f50057",
    },
    secondary: {
      main: "#2979ff",
    },
  },
});
